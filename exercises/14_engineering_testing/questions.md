# 十四、工程化、测试与调试

## 题 50：用 `assert` 写单元测试

给一个 `add` 函数和一个 `isPrime` 函数写简单测试，测试失败时程序应立即终止。

## 题 51：命令行参数解析

实现一个程序，支持 `--name Alice --age 18` 形式的命令行参数。

## 题 52：计时工具类

实现一个 RAII 风格的 `ScopedTimer`，构造时开始计时，析构时输出耗时。

## 题 53：异常安全转账

实现两个账户之间的转账函数，要求金额非法或余额不足时抛异常，并保持账户余额不被错误修改。
