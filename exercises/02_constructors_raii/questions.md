# 二、构造、析构与资源管理

### 题 4：观察构造和析构顺序

编写一个类 `Tracer`，构造时输出 `"construct"`，析构时输出 `"destroy"`。在函数内部创建局部对象，观察输出顺序。

### 题 5：禁止拷贝的类

设计一个 `NonCopyable` 类，允许默认构造，但不允许拷贝构造和拷贝赋值。

### 题 6：简单动态数组

实现一个管理动态数组的 `IntArray` 类，支持构造、析构、下标访问、获取大小。要求避免内存泄漏。
