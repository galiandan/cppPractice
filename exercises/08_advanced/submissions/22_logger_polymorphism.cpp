// 题 22：多态版日志系统
//
// 设计日志系统：
//
// - 抽象基类 `Logger`，提供 `log(const std::string&)`。
// - `ConsoleLogger` 输出到控制台。
// - `MemoryLogger` 保存日志到 `std::vector<std::string>`。
// - 使用 `std::unique_ptr<Logger>` 统一管理。

int main()
{
    return 0;
}
