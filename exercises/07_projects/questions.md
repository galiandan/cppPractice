# 七、综合项目题

### 题 19：图书管理系统

实现一个简化图书管理系统。要求：

- `Book` 类保存书名、作者、ISBN、是否借出。
- `Library` 类使用 `std::vector<Book>` 保存书籍。
- 支持添加图书、按 ISBN 借书、还书、按书名关键字搜索。

### 题 20：任务管理器

实现一个命令行任务管理器。要求：

- `Task` 包含编号、标题、优先级、完成状态。
- `TaskManager` 使用 STL 容器保存任务。
- 支持添加任务、完成任务、删除任务、按优先级排序输出未完成任务。
